import XCTest
@testable import Connectum

// Placeholder smoke test that proves the test target compiles and links against
// the app target during initial scaffolding. Feature tests are added per task.
final class ScaffoldSmokeTests: XCTestCase {
    func testScaffoldLinks() {
        XCTAssertTrue(true)
    }
}
