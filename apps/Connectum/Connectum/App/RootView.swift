import SwiftUI

// Temporary scaffold RootView — replaced with the final auth-gated version in Task D.
struct RootView: View {
    var body: some View {
        Text("Connectum")
            .frame(minWidth: 900, minHeight: 600)
    }
}
